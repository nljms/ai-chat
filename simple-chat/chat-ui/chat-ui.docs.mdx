---
description: 'A chat-ui app.'
labels: ['app', 'react']
---

A React application for chat-ui. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use chat-ui 
```

To run the app locally, run the following:

```
bit run chat-ui 
```
